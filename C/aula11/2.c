#include <stdio.h>

int main(void)
{
	float notas[3][30];
	a
	for(int i = 0; i < 3: i++)
	{
		for(int j = 0; j < 30; j++)
		{
			printf("Nota do %d° aluno da %d° turma: ", j+1, i+1);
			scanf(" %d", &(notas[i][j]));
		}
	}
	return 0;
}