#include <stdio.h>

int main(void)
{
	int soma = 0, reinciar = 0; cntnuar = 0;
	do
	{
		r
	}
	while(reinciar);
	do
	{
		int num = 0;
		printf("Número? ");
		scanf(" %d", &num);
		soma += num;
		printf("Continuar? (\"0\" para não, \"+\" sim) ");
		scanf(" %c", &cntnuar);
	}
	while(cntnuar_exec == '+');
	printf("Soma: %i\n", soma);

	return 0;
}