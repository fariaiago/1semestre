#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int soma = 0, nv_cmpra = 0; cntnuar = 0;
	do
	{
		r
	}
	while(nv_cmpra);
	do
	{
		int num = 0;
		printf("Número? ");
		scanf(" %s", &num);
		soma += num;
		printf("Continuar? (\"0\" para não, \"1\" sim) ");
		scanf(" %c", &cntnuar);
	}
	while(cntnuar_exec == '+');
	printf("Soma: %i\n", soma);

	return 0;
}